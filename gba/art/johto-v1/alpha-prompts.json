{
  "house_attempt_1": "Edit target: the attached generated house sprite. Preserve its silhouette, design, proportions and colors exactly. Remove the entire gray-and-white checkerboard background and replace it with GENUINE TRANSPARENT ALPHA, not a visual checkerboard pattern, white fill, or black fill. Keep the object itself opaque. Do not include any background decoration. Clean sharp pixel art edges. Return PNG with an actual alpha channel.",
  "house_final": "Edit target: attached house. Deliver this house as a cutout on a fully transparent background (RGBA PNG). All gray and white checkerboard pixels surrounding the house must be erased to zero alpha. Do not depict a checkerboard: the previous result failed because its checkerboard was opaque RGB. Preserve the house design. No glow, no shadow, no backdrop. A single opaque pixel-art house, transparent everywhere outside its outline.",
  "tree_final": "Edit target: the attached generated tree sprite. Preserve its silhouette, design, proportions and colors exactly. Remove the entire gray-and-white checkerboard background and replace it with GENUINE TRANSPARENT ALPHA, not a visual checkerboard pattern, white fill, or black fill. Keep the object itself opaque. Do not include any background decoration. Clean sharp pixel art edges. Return PNG with an actual alpha channel."
}
